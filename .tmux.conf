# tmux configuration

# Extend scrollback buffer history
set-option -g history-limit 50000

# Ghostty sends this private sequence for Cmd+L.
set-option -s user-keys[0] "\e[99~"
bind-key -n User0 detach-client
